
<div style ="margin:30px">
{{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_widget(formEquipe) }}
        {% if editMode %}
        <button  style='text-align-center' class="btn btn-primary">{{ button_label|default('Enregistrer modification') }}</button>
            {% else %}
        <button class="btn btn-primary">{{ button_label|default('+ Ajouter Match') }}</button>
        {% endif %}
    
{{ form_end(form) }}
</div>
