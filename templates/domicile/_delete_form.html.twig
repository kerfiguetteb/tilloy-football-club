<form method="post" action="{{ path('app_domicile_delete', {'id': domicile.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ domicile.id) }}">
    <button class="btn">Delete</button>
</form>
