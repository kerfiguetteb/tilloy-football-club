{% extends 'base.html.twig' %}

{% block title %}Domicile{% endblock %}

{% block body %}
    <h1>Domicile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domicile.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_domicile_index') }}">back to list</a>

    <a href="{{ path('app_domicile_edit', {'id': domicile.id}) }}">edit</a>

    {{ include('domicile/_delete_form.html.twig') }}
{% endblock %}
